export function Reconnecting() {
  return (
    <div className="absolute inset-0 bg-black/40 flex items-center justify-center z-50">
      <p className="font-plex text-sm text-surface tracking-widest uppercase">Reconnecting…</p>
    </div>
  )
}
